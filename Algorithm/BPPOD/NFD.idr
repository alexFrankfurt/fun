module Algorithm.BPPOD.NFD

import Data.So
import Data.SortedSet

import Algorithm.BPPOD.Model
import Algorithm.BPPOD.Property
import Data.Partition

data B

public export
interface Try where
  data A

Try where
 A = Int

-- BPPOD where
--   A = Double

--   l x = case choose (x > 0 && x < 1) of
--              Left p => [x;p]

--   partition = MkPartition empty [] ?rhs ?rhs3
